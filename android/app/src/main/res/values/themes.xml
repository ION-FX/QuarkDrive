<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose sets its own colours; this only removes the window background flash. -->
    <style name="Theme.Quarkdrive" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
